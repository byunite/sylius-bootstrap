{% import '@SyliusShop/Common/Macro/headers.html.twig' as headers %}

<div class="d-md-flex justify-content-between">
    {% set subTitle %}
        {{ 'sylius.ui.create_a_new_customer_account'|trans }}
    <a href="{{ path('sylius_shop_login') }}">{{ 'sylius.ui.have_an_account_already'|trans }} {{ 'sylius.ui.sign_in_here'|trans }}.</a>
    {% endset %}
    <div>{{ headers.default('sylius.ui.register_in_the_store'|trans, '', subTitle) }}</div>
</div>
